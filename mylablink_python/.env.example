# Example environment for local development
# Use console email backend for DEBUG development (prints emails to console)
DEBUG=True
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=MyLabLink <no-reply@mylablink.local>
SITE_BASE_URL=http://127.0.0.1:8000

# Example SMTP settings (uncomment and fill to use real SMTP)
# DEBUG=False
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=you@example.com
# EMAIL_HOST_PASSWORD=supersecret

# Allauth behavior
ACCOUNT_EMAIL_VERIFICATION=mandatory
ACCOUNT_EMAIL_CONFIRMATION_EXPIRE_DAYS=3